# M7.2 Assessment: Offer PR to Upstream (DrOlu/pi-a2a-communication)

**Prepared:** 2026-06-23  
**Decision needed by:** User  
**Recommendation:** Narrow PR with S1–S6b spec fixes only

---

## Upstream Status

| Metric | Value |
|--------|-------|
| Last pushed | 2026-03-15 (3+ months ago) |
| Open issues | 8 (6 filed by us, all unanswered) |
| Stars | 6 |
| Forks | 3 |
| Default branch | main |
| Recent activity | Gallery/docs changes only (v1.0.1) |

**No maintainer response to any of our 6 issues filed 2026-06-20.** The upstream appears inactive.

---

## Issues We Filed

| # | ID | Severity | Issue | Our Fix Commit |
|---|-----|----------|-------|----------------|
| #3 | S3 | HIGH | Wrong Agent Card discovery path (missing `.json` extension) | `fd3a23d`, `15afaf9` |
| #4 | S2 | HIGH | Missing `WWW-Authenticate` header on 401 responses | `fd3a23d` |
| #5 | S1 | MEDIUM | JSON-RPC errors return HTTP 400 instead of HTTP 200 | `cab19ea` |
| #6 | S5 | HIGH | Uncaught parse error in `/sendMessage` returns HTTP 500 | `fd3a23d` |
| #7 | S6+S6b | HIGH/LOW | Wrong JSON-RPC method names + wrong `id` in parse errors | `cab19ea` |
| #8 | S4 | HIGH | Missing v1.0 transport binding paths (`/rpc`, `/message:send`, `/message:stream`) | `d83f21e` |

**All 6 issues are A2A v1.0 spec compliance bugs.** All verified in our fork with 51/51 conformance tests passing.

---

## What Our Fork Adds Beyond Spec Fixes

Our fork has **73 total commits** vs upstream. The spec fixes represent ~7 commits. The remaining ~66 commits are our value-add:

| Category | Commits | Description |
|----------|---------|-------------|
| **Spec fixes (S1–S6b)** | ~7 | The narrow PR scope |
| **PiTaskBridge + SubprocessPiTaskBridge** | ~8 | Task execution via running pi session or subprocess |
| **PiSessionTaskHandler** | ~3 | `ctx.newSession()` based task handler with adaptive polling |
| **a2a_chain tool** | ~5 | Chain multiple A2A agents sequentially |
| **Broadcast/status improvements** | ~4 | `/a2a-broadcast`, `/a2a-status` commands |
| **Fleet deployment** | ~15 | Ansible playbooks, model profiles, bootstrap scripts |
| **Documentation** | ~25 | Wiki, AGENTS.md, architecture reports, FOCUS/PLAN |
| **Node-router → fleet-resource-manager** | ~5 | Benchmark migration, archival |

**The value-add is fleet-specific** — it depends on `pi` being installed, Ansible playbooks, model profiles, etc. This doesn't translate to a general-purpose upstream contribution.

---

## PR Options

### Option A: Narrow PR — Spec Fixes Only ✅ Recommended

**Scope:** 7 commits fixing S1–S6b (the 6 issues we filed)  
**Impact:** Any A2A v1.0 implementation benefits from these fixes  
**Risk:** Low — pure bug fixes, no new features, no API changes  
**Conflict risk:** Low — upstream hasn't changed relevant code since v1.0.1  
**Test coverage:** 51 conformance tests included  

**What it includes:**
- Fix S2: `WWW-Authenticate: Bearer` header on 401 responses
- Fix S3: `/.well-known/agent-card.json` spec path + legacy compat fallback
- Fix S5: try/catch around `JSON.parse` in `handleSendMessage`
- Fix S1: `sendJSONRPCError()` returns HTTP 200 instead of 400
- Fix S6: PascalCase method name mapping (`SendMessage`, `GetTask`, etc.) with backward compat for slash-separated
- Fix S6b: `id: null` instead of `id: 0` in parse errors
- Fix S4: `/rpc`, `/message:send`, `/message:stream` transport binding routes
- 51 conformance tests proving all fixes work

**What it does NOT include:** PiTaskBridge, a2a_chain, fleet deployment, model profiles, PiSessionTaskHandler, node-router migration, documentation changes.

### Option B: Broad PR — All Our Changes

**Scope:** All 73 commits  
**Impact:** Only useful to users running pi + fleet deployment  
**Risk:** High — large diff, fleet-specific dependencies, likely to be rejected or ignored  
**Conflict risk:** High — many unrelated changes, difficult to review  

### Option C: No PR — Maintain Fork Independently

**Scope:** No upstream contribution  
**Impact:** We maintain our fork with all improvements  
**Risk:** None — current state continues as-is  

---

## Assessment

| Factor | Option A (Narrow) | Option B (Broad) | Option C (No PR) |
|--------|-------------------|------------------|-------------------|
| Upstream response likelihood | Low (inactive) | Very low | N/A |
| Community benefit | High — spec fixes help everyone | Low — fleet-specific | None |
| Maintenance burden | Low — clean cherry-pick | High — merge conflicts | None |
| Our effort | 1–2 hours to prepare | 4–8 hours + ongoing | None |
| Risk of rejection | Low — pure bug fixes | High — too many changes | None |

**Recommendation: Option A.** The spec fixes are clean, well-tested, and directly address the 6 issues we filed. Even if the maintainer doesn't respond, a PR creates a public record that other A2A implementers can reference. Our value-add (PiTaskBridge, fleet deployment, etc.) stays in our fork where it belongs.

---

## Proposed PR Title & Description

**Title:** `fix: A2A v1.0 spec compliance — 6 conformance fixes (S1–S6b)`

**Description:**
> Fixes 6 A2A v1.0 spec compliance issues (#3–#8):
> 
> - **S1 (#5):** JSON-RPC errors return HTTP 200 instead of 400
> - **S2 (#4):** WWW-Authenticate header on 401 responses
> - **S3 (#3):** Agent Card discovery at `/.well-known/agent-card.json` with legacy fallback
> - **S4 (#8):** Transport binding routes `/rpc`, `/message:send`, `/message:stream`
> - **S5 (#6):** try/catch around JSON.parse in handleSendMessage
> - **S6+S6b (#7):** PascalCase method name mapping + `id: null` in parse errors
> 
> All fixes include backward compatibility for the previous behavior. 51 conformance tests included.
> 
> Verified in fork: carlosfrias/pi-a2a-communication v0.4.0

---

*Prepared for user review — decision needed on whether to offer PR to upstream.*